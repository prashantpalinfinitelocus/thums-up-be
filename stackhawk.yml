app:
  applicationId: thums-up-backend
  env: Production
  host: https://tccc-tja-test-cloudrun-backend-<hash>-<region>.a.run.app
  openApiSpec: ./docs/swagger.yaml

authentication:
  type: Bearer
  token: <your-test-jwt-token>

# Optional: Configure specific endpoints to test
# endpoints:
#   - path: /backend/api/v1/auth/send-otp
#     method: POST
#   - path: /backend/api/v1/profile
#     method: GET
#     auth: true

# Optional: Configure scan settings
# scanConfig:
#   activeScan: true
#   passiveScan: true
#   spider: true
#   ajaxSpider: true

